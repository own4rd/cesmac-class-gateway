server:
  port: 8084

spring:
  application:
    name: classes-api
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: ${KEYCLOAK_ISSUER_URI:http://localhost:8180/realms/cesmac}

management:
  endpoints:
    web:
      exposure:
        include: health,info
